# 🤖 تشغيل خوادم AI للـ 24Toolkit

## المتطلبات الأساسية

جميع المفاتيح محفوظة في Vercel، لكن للتطوير المحلي (`localhost`)، اتبع الخطوات التالية:

## الخطوة 1️⃣: إضافة مفاتيح API في `.env`

اختر أحد الخيارات التالية (أو أكثر):

### خيار A: Anthropic Claude (مستحسن)
1. توجه إلى https://console.anthropic.com/
2. احصل على مفتاح API
3. أضفه في `.env`:
```env
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxx
```

### خيار B: Groq (سريع وذكي)
1. توجه إلى https://console.groq.com/
2. احصل على مفتاح API
3. أضفه في `.env`:
```env
GROQ_API_KEY=gsk_xxxxxxxxxxxxx
```

### خيار C: GitHub Models (تجريبي)
1. توجه إلى https://github.com/settings/tokens
2. أنشئ Personal Access Token
3. أضفه في `.env`:
```env
GITHUB_TOKEN=ghp_xxxxxxxxxxxxx
```

## الخطوة 2️⃣: تشغيل خوادم التطوير

### الطريقة السهلة (تشغيل كل شيء معاً):
```bash
npm run dev:full
```

سيبدأ:
- ✅ API Server على `http://localhost:3000`
- ✅ Vite Dev Server على `http://localhost:5000` (أو `5001`)

### أو تشغيل منفصل:

**في Terminal 1 - الـ API Server:**
```bash
npm run api
```

**في Terminal 2 - الـ Vite Dev Server:**
```bash
npm run dev
```

## الخطوة 3️⃣: اختبار AI

1. افتح `http://localhost:5000` (أو `5001`)
2. اذهب إلى **AI Translator** أو **Chat Assistant**
3. جرب الأداة!

## استكشاف الأخطاء

### ❌ الخطأ: "Server configuration error: API key not configured"
- **الحل**: تأكد من إضافة المفتاح في `.env` وأعد تشغيل الـ API Server

### ❌ الخطأ: "Failed to process AI request"
- **الحل**: اختبر المفتاح من قائمة الـ AI Provider (اختر مزود مختلف)

### ❌ الخطأ: "Port 3000 already in use"
- **الحل**: اقتل العملية القديمة:
```bash
lsof -ti:3000 | xargs kill -9
```

## الملفات المهمة

- `server.js` - خادم API محلي
- `.env` - ملف البيئة (أضف مفاتيحك هنا)
- `vite.config.ts` - إعدادات Proxy للـ API
- `src/lib/ai.ts` - دالة `callAI` الرئيسية
- `api/ai.js` - دالة Vercel للإنتاج

## 🚀 نشر على Vercel

المفاتيح موجودة بالفعل في Vercel، لا تحتاج إلى أي شيء! فقط اضغط Deploy.
